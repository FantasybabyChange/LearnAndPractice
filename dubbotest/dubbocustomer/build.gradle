apply plugin:'application'
mainClassName='com.fantasybaby.gradle.customer.Consumer'
archivesBaseName = 'dubbocustomer'

dependencies{
    compile project(':dubboservice')
    compile(
            "org.apache.dubbo:dubbo"
    )
}